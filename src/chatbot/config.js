
import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  botName: "Travel",
  initialMessages: [
    createChatBotMessage(`Hello, Traveller. You're welcome to explore.`, 
    )
  ],
 
};

export default config;